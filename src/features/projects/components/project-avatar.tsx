type ProjectAvatarProps = {
  name: string;
  image: string;
  className?: string;
};

export default function ProjectAvatar({
  name,
  image,
  className,
}: ProjectAvatarProps) {
  return <div></div>;
}
